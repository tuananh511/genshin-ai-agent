# Genshin AI Account Manager
**Ngày:** {{ report_date }} | **Account:** {{ nickname }} | **AR:** {{ ar }}

---

## 📊 Character Score (xếp theo thứ tự nên build trước)

| Nhân vật | Lv | Cung mệnh | Vũ khí | Set | ATK% | HP% | DEF% | EM | ER% | Crit Rate% | Crit DMG% | DMG Bonus | Vấn đề |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
{% for s in scores -%}
| {{ s.name }} ({{ s.element_vi }}) | {{ s.level }} | C{{ s.constellation_count }} | {% if s.weapon_icon_url %}<img src="{{ s.weapon_icon_url }}" width="20" height="20"> {% endif %}{{ s.weapon_name }} | {% for set in s.sets %}{% if set.icon_url %}<img src="{{ set.icon_url }}" width="20" height="20"> {% endif %}{{ set.name }} ({{ set.count }}){% if not loop.last %}, {% endif %}{% endfor %} | {{ s.stat_values[0] }} | {{ s.stat_values[1] }} | {{ s.stat_values[2] }} | {{ s.stat_values[3] }} | {{ s.stat_values[4] }} | {{ s.stat_values[5] }} | {{ s.stat_values[6] }} | {{ s.dmg_bonus }} | {% if s.artifact_issue_count %}{{ s.artifact_issue_count }} artifact{% endif %}{% if s.low_talent_names %} / Cần nâng: {{ s.low_talent_names | join(', ') }}{% endif %}{% if not s.artifact_issue_count and not s.low_talent_names %}✅{% endif %} |
{% endfor %}

---

## 🔍 Phân tích từ AI

{{ llm_advice }}

---

## 📖 Chi tiết Build Guide

{% for acc in guide_accordions %}
<details>
<summary>{{ acc.name }} ({{ acc.element_vi }})</summary>

{% if acc.url %}*Đây là bản tóm tắt. Xem chi tiết đầy đủ tại: [trang build {{ acc.name }}]({{ acc.url }})*{% endif %}

- **Vũ khí nên dùng (cao → thấp):** {% for w in acc.weapons %}{% if w.icon_url %}<img src="{{ w.icon_url }}" width="20" height="20"> {% endif %}{{ w.name }}{% if not loop.last %}, {% endif %}{% endfor %}
- **Bộ Thánh Di Vật nên dùng:** {% for set in acc.artifact_sets %}{% if set.icon_url %}<img src="{{ set.icon_url }}" width="20" height="20"> {% endif %}{{ set.label }}{% if not loop.last %}, {% endif %}{% endfor %}
- **Thuộc tính ưu tiên của TDV:** {% for slot, stat in acc.main_stats.items() %}{{ slot }}: {{ stat }}{% if not loop.last %}, {% endif %}{% endfor %}
- **Ưu tiên chỉ số phụ:** {{ acc.substats | join(" > ") }}
- **Ưu tiên kỹ năng:** {{ acc.talents | join(" > ") }}

</details>
{% endfor %}

---
*Generated by Genshin AI Agent*