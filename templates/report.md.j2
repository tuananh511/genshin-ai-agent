# Genshin AI Account Manager
**Ngày:** {{ report_date }} | **Account:** {{ nickname }} | **AR:** {{ ar }}

---

## 📊 Character Score (xếp theo thứ tự nên build trước)

| Nhân vật | Lv | C | ATK% | HP% | DEF% | EM | ER% | Crit Rate% | Crit DMG% | DMG Bonus | Vấn đề |
|---|---|---|---|---|---|---|---|---|---|---|---|
{% for s in scores -%}
| {{ s.name }} ({{ s.element }}) | {{ s.level }} | C{{ s.constellation_count }} | {{ s.stat_values[0] }} | {{ s.stat_values[1] }} | {{ s.stat_values[2] }} | {{ s.stat_values[3] }} | {{ s.stat_values[4] }} | {{ s.stat_values[5] }} | {{ s.stat_values[6] }} | {{ s.dmg_bonus }} | {% if s.artifact_issue_count %}{{ s.artifact_issue_count }} artifact{% endif %}{% if s.low_talent_names %} / Cần nâng: {{ s.low_talent_names | join(', ') }}{% endif %}{% if not s.artifact_issue_count and not s.low_talent_names %}✅{% endif %} |
{% endfor %}

---

## 🔍 Phân tích từ AI

{{ llm_advice }}

{% if guide_sources %}**Nguồn tham khảo:** {% for g in guide_sources %}[{{ g.name }}]({{ g.url }}){% if not loop.last %}, {% endif %}{% endfor %}{% endif %}

---

## 🎯 Domain nên farm hôm nay ({{ day_of_week }})

{% for todo in required_todos -%}
- **{{ todo.label }}** — {{ todo.reason }}
{% endfor %}


## 📝 Nhắc nhở thêm (tuỳ chọn)

{% for todo in optional_todos -%}
- {{ todo.label }} — {{ todo.reason }}
{% endfor %}

---

---
*Generated by Genshin AI Agent*